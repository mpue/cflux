# Time Tracking System - Umgebungsvariablen Template
# WICHTIG: Kopiere diese Datei zu .env und ersetze die Platzhalter mit sicheren Werten!
# Die .env Datei ist in .gitignore und wird NICHT ins Repository committed.

# PostgreSQL Database
POSTGRES_USER=timetracking
POSTGRES_PASSWORD=ERSETZE_MIT_SICHEREM_PASSWORT
POSTGRES_DB=timetracking

# Backend JWT Configuration
# Generiere sicheren Secret mit PowerShell:
# -Join ((1..48) | ForEach-Object { [char](Get-Random -Input ((48..57) + (65..90) + (97..122))) })
JWT_SECRET=ERSETZE_MIT_MINDESTENS_32_ZUFÄLLIGEN_ZEICHEN
JWT_EXPIRES_IN=7d
PORT=3001
NODE_ENV=production

# CORS Configuration (komma-getrennte Liste erlaubter Origins)
# Development: http://localhost:3000,http://localhost:3002
# Production: https://deine-domain.com,https://www.deine-domain.com
CORS_ORIGIN=http://localhost:3000,http://localhost:3002

# API URL for Frontend
REACT_APP_API_URL=http://localhost:3001/api
